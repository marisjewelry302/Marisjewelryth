# Structure Reports

Generated project tree snapshots live here so the repository root stays focused on deploy, package, and source files.

- `tree_WEBSITE.txt`: full project tree snapshot
- `public-tree.txt`: public folder tree snapshot placeholder

Regenerate these only when a fresh structure snapshot is useful for debugging or handoff.
